# Amorsa fiduciae

Directorium `bootstrap/python/` compilatorem Python minimum continet. E fonte
VINDEX compilatorem nativum restituit et sic fiduciam in binario tradito
minuit.

```bash
./bootstrap/reconstruit.sh
```

Catena canonica 0.53:

1. `bootstrap/python/compilateur_053.py` fontem compilatoris VINDEX in amorsam
   nativam convertit;
2. compilator amorsae eundem fontem in generationem nativam primam convertit;
3. generatio prima secundam producit;
4. generationes nativae byte pro byte comparantur;
5. punctum fixum cum `compilator_vindex` distributo comparatur.

Punctum fixum hodiernum est:

```text
166a0e666deb83f759f90d1b721474ede01bb3519ec5231b2fe0e9b23158c969
```

Exitus ad viam electam scribi potest:

```bash
./bootstrap/reconstruit.sh ./compilator_reconstructus
```

Amorsa 0.53 conventionem System V septem argumentorum probat; casus
`argumenta_septem.vindex` exitum `28` reddit. Workflow canonicus hanc
probationem separatim exsequitur.

Instrumenta Python ad amorsam, verificatores facultativos et probationes
pertinent. Lingua, compilator nativus, programmata iam compilata et Officina
nativa Python in tempore executionis non requirunt.

Amorsa ipsum backend ELF ad catenae fiduciae reconstructionem adhibet. Backend
PE/Win64 a compilatore VINDEX auto-hospite generatur et workflow separato sub
Windows vero probatur.

**VINDEX Latine cogitat. Sylvia Latine loquitur.**
